{
  "name": "poorman-alpha",
  "version": "0.2.0",
  "private": true,
  "description": "Computational math plugin: unit conversion, symbolic algebra, matrix operations, SymPy bridge with step-by-step and plot support.",
  "main": "index.js",
  "scripts": {
    "test": "jest --verbose",
    "setup": "node setup-venv.js",
    "setup:check": "node setup-venv.js --check",
    "setup:clean": "node setup-venv.js --clean",
    "postinstall": "node setup-venv.js"
  },
  "dependencies": {
    "mathjs": "^13.2.0",
    "nerdamer": "^1.1.13"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
