# HTML Artifact Studio Plugin

Generate, preview, and edit HTML/React artifacts.

## Permissions
- `dsn:register-tool`
- `fs:read`
- `fs:write`

## Usage
This plugin provides a `generate_artifact` tool to the agent and a dashboard for managing artifacts.
